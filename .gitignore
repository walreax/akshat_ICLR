# Python
__pycache__/
*.pyc
.ipynb_checkpoints/

# Node / Next.js (human_eval_app)
node_modules/
.next/

# Logs (regenerated by every run, not source)
*.log

# Model weights / SAE checkpoints (regenerable via train_sae.py etc, too large for git)
*.pt
*.safetensors
*.ckpt

# Large generated outputs / raw activation dumps (regenerable by the extractor scripts)
flux_output/
pixart_output/
sd3_output/
imageMetric/flux_output/
imageMetric/pixart_output/
imageMetric/sd3_output/
flux_results/
sd_activations/
sae_checkpoints_uploaded/
sae_checkpoints_4200/
sae_checkpoints_provided/

# In-progress remote extraction runs (large .npz latent dumps, not finalized)
cfs_*_calib/
cfs_*_test/

# Human-eval app: generated stimulus images (thousands of files, GBs) -- keep the prompts CSV
human_eval_app/public/images/flux/
human_eval_app/public/images/pixart/
human_eval_app/public/images/sd3/

# OS / editor junk
.DS_Store
Thumbs.db
.vscode/

# Claude Code session/tooling config (local machine state, not project source)
.claude/

# Scratch pulls / one-off debug scripts / analysis dumps (not project source)
scratch_pull_599/
scratch_repro_*.py
scratch_url_failure.txt

# Raw human-annotator data (real participant judgments -- not for the code repo)
scratch_human_results.csv
scratch_human_aggregated.csv
